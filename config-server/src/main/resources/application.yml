server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tonyblack10/microservices-repo
          search-paths: fornecedor, loja, transportador
